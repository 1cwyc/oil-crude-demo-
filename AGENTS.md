# Codex 项目规则

本文件适用于仓库根目录及全部子目录。详细工作流见 [CONTRIBUTING.md](CONTRIBUTING.md)，数据边界见 [docs/DATA_BOUNDARIES.md](docs/DATA_BOUNDARIES.md)。

## 开始工作前

1. 先读取 `README.md`、当前任务 PR、`docs/MODULES.md` 和相关 v0.2 合同章节。
2. 读取目标模块的现有代码、测试和配置，不凭文档猜测真实实现。
3. 一次只向用户提出一个问题，直到对真实需求和成功标准有至少 95% 把握。
4. 将任务分为：不做就无法实现目标、非必要但能显著改善结果、当前没有足够收益；完成第一类，砍掉第三类，对第二类明确取舍。
5. 明确区分已核实事实、基于证据的推断/判断和暂时无法核实的信息。

## 开发门禁

- 开发业务功能前先检查仓库并浏览相关 GitHub 开源实现，再编写包含需求、模块、技术方案、目录、风险和验收的 PRD。
- PRD 必须经用户确认；随后编写实施计划，使用测试驱动开发，不得先写生产代码。
- 模块保持单一职责；公共能力只有被两个以上模块以完全相同语义使用时才进入共享层。
- 不复制已有字段、输出表、配置加载、日志、路径、manifest、哈希或 Parquet 写入实现。
- 发现用户假设或指令存在技术问题时，必须基于证据直接指出。

## Git 与双主机接力

- GitHub 公共仓库是唯一代码源。每台主机独立克隆并使用独立 SSH 密钥。
- 开始任务前更新 `main`：`git pull --ff-only origin main`。
- 每项任务使用一个分支和一个 PR；两台主机不得同时修改同一任务分支。
- 不直接推送、强推或删除 `main`。不使用 `git reset --hard` 或其他破坏性恢复命令。
- 交接前将命令、测试结果、风险和当前状态写入 PR，并停止修改该分支。

## 数据安全

- 不提交真实 AIS、生成结果、ChinaPorts 原始响应、Cookie、凭据、密钥或主机专属路径。
- 仓库测试和 CI 只使用 `sample_data/` 中不超过 64 KiB 的合成 AIS 样本。
- 输入 schema 与最小合同不一致时，只阻断相关模块并报告差异，不静默补字段或猜测类型。
- 上游 AIS 和已有正式 Parquet 只读；新增模块只写自己的输出目录。

## 命令可见性

在 Windows 上执行 cmd 或 PowerShell 操作时，应让用户看到命令或窗口及关键输出；不得以静默后台方式隐藏长时间安装、迁移或数据运行。
